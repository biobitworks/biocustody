import * as React from "react";
import { createRoot } from "react-dom/client";
import { BioCustodyApp } from "./App";

const root = createRoot(document.getElementById("root")!);
root.render(<BioCustodyApp />);
